<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.imef.uiwidget">

    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".UiEntryActivity"/>
        <activity android:name=".TextInputLayoutActivity"/>
        <activity android:name=".DrawerLayoutActivity"/>
        <activity android:name=".adapter.TextureActivity"/>
        <activity android:name=".SnackBarActivity"/>
        <activity android:name=".NavigationActivity"/>
        <activity android:name=".SearchViewActivity"/>
        <activity android:name=".CustomViewActivity"/>

    </application>

</manifest>